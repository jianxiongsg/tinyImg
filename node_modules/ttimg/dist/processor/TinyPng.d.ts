import * as rf from "remotefile";
import { ImgUtilParams } from "../ImgUtilServer";
export declare class TinyPng {
    private keys;
    private curKeyIndex;
    private storage;
    private waitQueue;
    constructor(keys: string[], folder?: string);
    private changeKey;
    private _processFile;
    private getFileName;
    processFile(file: string, toFile?: string): Promise<string>;
    next(): void;
    add(file: string): Promise<string>;
    process(files: rf.FileDict, params: ImgUtilParams): Promise<rf.RemoteFileResInfo>;
}
