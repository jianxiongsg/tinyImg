import * as rf from "remotefile";
export interface ImgUtilParams {
    type: UtilType;
}
export declare enum UtilType {
    TexturePacker = 0,
    TinyPng = 1
}
interface Processor {
    process(files: rf.FileDict, params: ImgUtilParams): Promise<rf.RemoteFileResInfo>;
}
export declare class ImgUtilServer {
    private server;
    private processor;
    constructor(port: number, uploadFolder?: string);
    registerType(type: UtilType, processor: Processor): void;
    private process;
    close(): void;
}
export {};
