export declare class MD5Storage {
    private folder;
    /**
     * @param folder 存储路径
     */
    constructor(folder: string);
    /**
     * 获得path文件的存储路径
     * @param path
     */
    getPath(path: string): string;
    /**
     * 用path的md5值，保存path指定文件到存储里
     * @param path
     */
    save(path: string): void;
    /**
     * 用path的md5值，保存file指定文件到存储里
     * @param path
     * @param file
     */
    saveOther(path: string, file: string): void;
    getValidPath(path: string): string | undefined;
    /**
     * 读取path文件对应的md5文件，没有则返回undefined
     * @param path
     */
    load(path: string): Buffer | undefined;
}
