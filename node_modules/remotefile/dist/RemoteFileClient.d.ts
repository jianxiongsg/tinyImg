import { FileDict } from "./RemoteFileServer";
export interface RemoteFile {
    err?: string;
    files?: {
        [name: string]: Buffer;
    };
    params?: any;
}
export declare class RemoteFileClient {
    private url;
    constructor(url: string);
    private recieveData;
    private extractData;
    process(files: FileDict, params?: any): Promise<RemoteFile>;
}
