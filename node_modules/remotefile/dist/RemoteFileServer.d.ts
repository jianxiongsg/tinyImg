export interface FileDict {
    [fieldname: string]: string;
}
export interface RemoteFileResInfo {
    err?: string;
    files?: FileDict;
    params?: any;
}
export interface ServerOption {
    uploadFolder?: string;
    cleanUpTime?: number;
}
export declare class RemoteFileServer {
    private port;
    private uploadFolder;
    private cleanUpTime;
    private server;
    private timeHandler;
    private processor;
    constructor(port: number, processor: (files: FileDict, params?: any) => Promise<RemoteFileResInfo>, option?: ServerOption);
    private message;
    private start;
    clean(): void;
    close(): void;
}
